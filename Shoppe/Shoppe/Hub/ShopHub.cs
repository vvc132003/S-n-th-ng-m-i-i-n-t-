using Microsoft.AspNetCore.SignalR;

public class ShopHub : Hub
{
   /* public async Task NotifyShopAdded(string shopName)
    {
        await Clients.All.SendAsync("ReceiveShopAdded", shopName);
    }*/ 
}
